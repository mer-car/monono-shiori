'use strict';
(()=>{let data,lastSeed;const seedNow=()=>Math.floor(Date.now()/3600000);function random(seed){let s=seed>>>0;return()=>{s+=0x6D2B79F5;let t=s;t=Math.imul(t^t>>>15,t|1);t^=t+Math.imul(t^t>>>7,t|61);return((t^t>>>14)>>>0)/4294967296;};}function shuffle(items,rng){const a=items.slice();for(let i=a.length-1;i>0;i--){const j=Math.floor(rng()*(i+1));[a[i],a[j]]=[a[j],a[i]];}return a;}function select(seed){const rng=random(seed),groups=['trading-cards','characters','hobby'],used=new Set(),topics=new Set();const queues=Object.fromEntries(groups.map(g=>[g,shuffle(data.filter(a=>a.group_slug===g),rng)]));function take(g){const q=queues[g];let a=q.find(a=>!used.has(a.record_id)&&!topics.has(a.rotation_group))||q.find(a=>!used.has(a.record_id));if(a){used.add(a.record_id);topics.add(a.rotation_group);}return a;}const picks=[];for(let i=0;i<6;i++)picks.push(take(groups[i%3]));const sections={};for(const g of groups){sections[g]=[];for(let i=0;i<4;i++)sections[g].push(take(g));}return{picks,sections};}function story(a){const el=document.createElement('article');el.className='story';const meta=document.createElement('div');meta.className='story-meta';meta.textContent=a.topic;const h=document.createElement('h3'),link=document.createElement('a');link.href=a.path;link.textContent=a.title;h.append(link);const p=document.createElement('p');p.textContent=a.description;el.append(meta,h,p);return el;}function render(){const seed=seedNow();if(seed===lastSeed||!data)return;if(document.activeElement?.closest('#picks,[id^="home-"]'))return;const chosen=select(seed);document.querySelector('#picks').replaceChildren(...chosen.picks.map(story));for(const[g,a]of Object.entries(chosen.sections))document.getElementById('home-'+g).replaceChildren(...a.map(story));lastSeed=seed;}fetch('assets/articles.json').then(r=>{if(!r.ok)throw Error();return r.json();}).then(a=>{data=a;render();setInterval(render,30000);}).catch(()=>{/* 固定HTMLがそのまま閲覧できる */});})();
