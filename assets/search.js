'use strict';
(()=>{const form=document.getElementById('search-form'),input=document.getElementById('query'),status=document.getElementById('search-status'),results=document.getElementById('results');let data;const normalize=s=>s.normalize('NFKC').toLowerCase();function run(){if(!data)return;const q=input.value.trim().slice(0,100),words=normalize(q).split(/\s+/).filter(Boolean);results.replaceChildren();if(!words.length){status.textContent='商品名やシリーズ名を入力してください。';return;}const found=data.filter(a=>words.every(w=>normalize(a.title+' '+a.keyword+' '+a.topic).includes(w)));status.textContent='「'+q+'」の記事：'+found.length+'件';for(const a of found){const el=document.createElement('article');el.className='story';const meta=document.createElement('div');meta.className='story-meta';meta.textContent=a.topic;const h=document.createElement('h3'),link=document.createElement('a');link.href=a.path;link.textContent=a.title;h.append(link);el.append(meta,h);results.append(el);}}form.addEventListener('submit',e=>{e.preventDefault();run();});fetch('assets/articles.json').then(r=>{if(!r.ok)throw Error();return r.json();}).then(a=>{data=a;run();}).catch(()=>{status.textContent='記事一覧からお探しください。検索データを読み込めませんでした。';});})();
